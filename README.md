# Homebrew Tap for KeySwitch

Install KeySwitch with Homebrew:

```sh
brew tap ygf2703/keyswitch
brew install --cask keyswitch-lite
```

Install KeySwitch Premium:

```sh
brew install --cask keyswitch
```

KeySwitch requires macOS Accessibility permission:

```text
System Settings > Privacy & Security > Accessibility
```

